1.删除有内容的文件夹
	rm -rf <name>
2.查看隐藏文件
	ls -ah
3.source <file>
	执行脚本
4.重命名
	mv <file> <new_name>	//和移动一样
5.切换用户/超级用户
	（1)su	//切换到超级用户
	（2)su 用户名	//切换到用户
	（3)有的电脑root用户没有密码，通过sudo passwd root设置
	（4)切换用户后，~/.bashrc等文件可能会变，即环境可能需要重新配置
